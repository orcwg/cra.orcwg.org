<footer>
    {% for section in site.footer.sections %}
    <section>
        <h4>{{ section.title }}</h4>
        {% if section.content %}
            {{ section.content | markdown | safe }}
        {% endif %}
        {% if section.title == "About" %}
            <p>Last build: {{ build.formatted }}</p>
        {% endif %}
        {% if section.list %}
            <ul>
            {% for item in section.list %}
                <li>{{ item | markdownInline | safe }}</li>
            {% endfor %}
            {% if section.title == "Community Resources" %}
                <li>
                    <a class="admin-toggle" title="Toggle admin mode" aria-label="Toggle admin mode">
                        <span class="toggle-text-off">Show admin info</span>
                        <span class="toggle-text-on">Hide admin info</span>
                    </a>
                </li>
            {% endif %}
            </ul>
        {% endif %}
    </section>
    {% endfor %}
</footer>
