{#
  FAQ Accordion Item Component

  Renders a single FAQ as a collapsible accordion item.
  Used on list pages to show multiple FAQs.

  Parameters:
  - faq: FAQ object with question, answer, etc.
  - open: whether the item should display as open
#}

<details class="faq-accordion-item" {% if open %}{{ "open" }}{% endif %}>
  <summary>
    <span class="question-text">
      {% if faq.questionNumber and list._showQuestionNumbers %}
        {{ faq.questionNumber }}
      {% endif %}
      {{ faq.questionHtml | safe }}
    </span>
  </summary>

  <article>
    <div>
      {{ faq.answerHtml | safe }}

      {% set item = faq %}
      {% include "components/faq/faq-attribution.njk" %}
      {% include "components/faq/faq-warning.njk" %}
      {% include "components/common/actions.njk" %}
    </div>
  </article>
</details>