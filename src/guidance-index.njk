---
layout: layout.njk
title: "Pending Guidance Requests"
permalink: "/pending-guidance/"
---

<section class="section-intro">
  <p>
    The impact of the Cyber Resilience Act raises questions that require ongoing dialogue with the European Commission. One of the most effective channels for these discussions is the CRA Experts Group, of which the Eclipse Foundation is a member. Topics are raised within the relevant work strands and addressed during working sessions. The documents presented here reflect the current state of these conversations and our level of confidence in their outcomes.
  </p>
</section>

<nav class="card-grid">
  {% for item in data.guidance %}
    <a href="{{ item.permalink }}" class="card">
      <h2>{{ item.title | markdownInline | safe }}</h2>
      {% if item.data.status %}
        <p><strong>Status:</strong> {{ item.data.status | replace('-', ' ') | title }}</p>
      {% endif %}
      {% if item.relatedFaqs and item.relatedFaqs.length > 0 %}
        <p>{{ item.relatedFaqs.length }} related FAQ{{ 's' if item.relatedFaqs.length != 1 }}</p>
      {% endif %}
    </a>
  {% endfor %}
</nav>
