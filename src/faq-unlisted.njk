---
layout: layout.njk
title: Unlisted FAQs
permalink: "/faq/unlisted/"
---

<section class="section-intro">
  <p>FAQs that exist as files but haven't been added to any category list yet</p>
  {% if data.unlistedFaqs.length == 0 %}
    <p>
      <strong>Good news !</strong>
      All FAQs are included in category lists.</p>
  {% endif %}
</section>

{% if data.unlistedFaqs.length > 0 %}
  <section class="section-card">
    {% set componentData = {
      title: "Unlisted FAQs",
      descritption: "Files not referenced in any README.yml list",
      faqs: data.unlistedFaqs,
      icon: "📋"
    } %}
    {% include "components/faq-list-simple.njk" %}
  </section>
{% endif %}