---
layout: layout.njk
title: FAQ lists
permalink: "/faq/"
---

<section class="section-intro">
  <p>Lists of FAQs organized around specific audiences or topics.</p>
</section>

<nav class="card-grid">
  {% for listId in site.listOrder %}
    {% for list in data.lists %}
      {% if list.id == listId %}
        <a href="{{ list.permalink }}" class="card">
          <h2>{% if list.icon %}{{ list.icon }} {% endif %}{{ list.title }}</h2>
          <p>{{ list.description }}</p>
        </a>
      {% endif %}
    {% endfor %}
  {% endfor %}
  {% if data.unlistedFaqs.length > 0 %}
    <a href="/faq/unlisted/" class="card">
      <h2>📋 Unlisted FAQs</h2>
      <p>{{data.unlistedFaqs.length}} FAQ{{ 's' if data.unlistedFaqs.length != 1}} not in any list.</p>
    </a>
  {% endif %}
</nav>
