---
layout: layout.njk
title: "Curated FAQ Lists"
permalink: "/lists/"
listOrder:
  - cra-itself
  - projects
  - maintainers
  - stewards
  - manufacturers
  - standards
  - important-and-critical-products
  - attestations
  - legislation
---

<div class="curated-lists-intro">
  <h3>⚠️ These lists are AI generated examples</h3>
  <p>Carefully curated collections of FAQs for specific topics and audiences. These lists only include complete, approved FAQs without missing content.</p>
</div>

<div class="curated-lists-grid">
  {% for listKey in listOrder %}
    {% for listItem in curatedLists %}
      {% if listItem.key == listKey and listItem.value.count > 0 %}
        <a href="/lists/{{ listItem.key }}/" class="curated-list-card">
          <h2 class="list-title">{{ listItem.value.title }}</h2>
          <p class="list-description">{{ listItem.value.description }}</p>
        </a>
      {% endif %}
    {% endfor %}
  {% endfor %}
</div>

