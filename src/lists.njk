---
layout: layout.njk
title: "Curated FAQ Lists"
permalink: "/lists/"
listOrder:
  - cra-itself
  - projects
  - maintainers
  - stewards
  - manufacturers
  - standards
  - important-and-critical-products
  - attestations
  - legislation
---

<div class="curated-lists-intro">
  <h3>Curated FAQ lists</h3>
  <p>Curated collections of FAQs for specific topics and audiences. These lists includes questions and answers covering all aspects of the Cyber Resilience Act as it relates to open source.</p>
  <p><strong>Note:</strong> These list includes FAQs regardless of their completion status. Some entries may be drafts, have missing content, or require additional guidance.
</div>

<div class="curated-lists-grid">
  {% for listKey in listOrder %}
    {% for listItem in curatedLists %}
      {% if listItem.key == listKey and listItem.value.count > 0 %}
        <a href="/lists/{{ listItem.key }}/" class="curated-list-card">
          <h2 class="list-title">
            {% if listItem.value.icon %}
              <span class="list-icon">{{ listItem.value.icon }}</span>
            {% endif %}
            {{ listItem.value.title }}
          </h2>
          <p class="list-description">{{ listItem.value.description }}</p>
        </a>
      {% endif %}
    {% endfor %}
  {% endfor %}
</div>

