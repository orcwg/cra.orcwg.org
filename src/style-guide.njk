---
layout: layout.njk
title: Style Guide
permalink: "/style-guide/"
eleventyExcludeFromCollections: true
---

<section class="section-intro">
  <h1 class="section-header">Living Style Guide</h1>
  <p>This page demonstrates all design patterns and components using semantic HTML with minimal component classes.</p>
  <p>Edit <code>_data/style-guide-content.md</code> or <code>_data/styleGuideData.js</code> to update examples.</p>
</section>

{# ============================================================================
   MARKDOWN CONTENT
   ============================================================================ #}

<section class="section-card">
  <h2>Markdown Content Examples</h2>
  {{ styleGuideData.markdownContent | markdown | safe }}
</section>

{# ============================================================================
   FAQ & LIST COMPONENTS - All using sampleRecursiveList for consistency
   ============================================================================ #}

<section class="section-card">
  <h2>FAQ Accordion Component</h2>
  <p>Component: <code>components/faq/faq-accordion-item.njk</code></p>
  <p>Native HTML accordions with CSS-only animations using <code>&lt;details&gt;</code> elements.</p>
  <p>Showing FAQs from the same sample list used in other views below.</p>

  {% for item in styleGuideData.sampleRecursiveList.children %}
    {% if item.type == 'faq' %}
      {% set faq = item %}
      {% include "components/faq/faq-accordion-item.njk" %}
    {% endif %}
  {% endfor %}
</section>

<section class="section-card">
  <h2>List Flat View (With Context)</h2>
  <p>Component: <code>components/faq/list-flat-with-context.njk</code></p>
  <p>Shows list title, description, and immediate children (no recursion). Same data as above and below.</p>
</section>

{% set list = styleGuideData.sampleRecursiveList %}
{% include "components/faq/list-flat-with-context.njk" %}

<section class="section-card">
  <h2>List Expanded View (Recursive) with Badges</h2>
  <p>Component: <code>components/faq/list-expanded-items.njk</code></p>
  <p>Recursively renders nested list structures with alternating backgrounds. Same data as above.</p>
  <p><strong>Status Badges:</strong> FAQs display status badges (Component: <code>components/faq/faq-badges.njk</code>) that use <code>&lt;data&gt;</code> elements with semantic values. Use the toggle below to show/hide badges:</p>

  <p>
    <button type="button" onclick="document.querySelectorAll('.nested-faq-list .badges').forEach(b => b.toggleAttribute('hidden'))">
      Toggle Badge Visibility
    </button>
  </p>
</section>

<section class="section-card nested-faq-list">
  {% set list = styleGuideData.sampleRecursiveList %}
  {% set styleClass = 'even' %}
  {% include "components/faq/list-expanded-items.njk" %}
</section>

{# ============================================================================
   CARD COMPONENTS
   ============================================================================ #}

<section class="section-card">
  <h2>List Card Component</h2>
  <p>Component: <code>components/list-card.njk</code></p>
  <p>Displays nested lists as clickable cards with FAQ/list counts.</p>
  <p>Showing nested lists from the sample data.</p>

  {# Show all nested lists from sampleRecursiveList #}
  {% for item in styleGuideData.sampleRecursiveList.children %}
    {% if item.type == 'list' %}
      <div class="list-card-wrapper">
        {% set list = item %}
        {% include "components/list-card.njk" %}
      </div>
    {% endif %}
  {% endfor %}
</section>

{# ============================================================================
   NAVIGATION CARDS (Hard-coded examples for design patterns)
   ============================================================================ #}

<section class="section-card">
  <h2>Navigation Cards</h2>
  <p>Used on home page and list indexes with hover effects and arrows. Uses <code>.card-grid</code> and <code>.card</code> classes.</p>

  <nav class="card-grid">
    <a href="#" class="card">
      <h3>Example Card 1</h3>
      <p>This is a navigation card with hover effects and an animated arrow</p>
    </a>
    <a href="#" class="card">
      <h3>Example Card 2</h3>
      <p>Cards use the <code>.card</code> class for consistent styling</p>
    </a>
    <a href="#" class="card">
      <h3>Example Card 3</h3>
      <p>Grid layout responds to screen size automatically</p>
    </a>
  </nav>
</section>

<section class="section-card">
  <h2>Half-Width Cards</h2>
  <p>Cards can be displayed in a two-column layout using <code>.card-grid.half-width</code>.</p>

  <nav class="card-grid half-width">
    <a href="#" class="card">
      <h3>Half Width 1</h3>
      <p>Two columns on larger screens, with arrows positioned at bottom-right</p>
    </a>
    <a href="#" class="card">
      <h3>Half Width 2</h3>
      <p>Responsive layout adapts to screen size</p>
    </a>
  </nav>
</section>

{# ============================================================================
   SEMANTIC ELEMENTS
   ============================================================================ #}

<section class="section-card">
  <h2>Disclaimer/Warning Block</h2>
  <p>Uses semantic <code>&lt;aside&gt;</code> element.</p>

  <aside class="warning">
    <h3>Important Notice</h3>
    <p><strong>This is an example warning block.</strong> It uses an <code>&lt;aside&gt;</code> element with automatic styling for disclaimers and important notices.</p>
  </aside>
</section>


{# ============================================================================
   CALLOUT ELEMENT
   ============================================================================ #}

<section class="section-card">
  <h2>Callout Block</h2>

  {% set content = "This is a example of content you can have in a Callout Block." %}
  {% set type = 'orc-recommendation' %}
  {% set title = "Title of the Callout" %}
  {% set graph = '/assets/images/orcwg_orc_presents.png' %}
  {% include "components/callout.njk" %}
</section>
   

{# ============================================================================
   DOCUMENTATION
   ============================================================================ #}

<section class="section-card">
  <h2>Semantic HTML Structure</h2>
  <p>The templates use semantic HTML5 elements with minimal component classes:</p>
  <ul>
    <li><code>&lt;header class="site-header"&gt;</code> - Site header with logo and navigation</li>
    <li><code>&lt;nav class="site-nav"&gt;</code> - Navigation menu links</li>
    <li><code>&lt;nav class="card-grid"&gt;</code> - Card navigation grids</li>
    <li><code>&lt;main&gt;</code> - Main content area</li>
    <li><code>&lt;section class="section-card"&gt;</code> - Content boxes and FAQ categories</li>
    <li><code>&lt;section class="section-intro"&gt;</code> - Intro/description sections</li>
    <li><code>&lt;article class="section-card"&gt;</code> - FAQ answers and standalone content</li>
    <li><code>&lt;ul class="link-list"&gt;</code> - Styled link lists with badges</li>
    <li><code>&lt;aside&gt;</code> - Disclaimers and warnings (no class needed)</li>
    <li><code>&lt;details&gt;</code> + <code>&lt;summary&gt;</code> - Native HTML accordions</li>
    <li><code>&lt;data class="badge"&gt;</code> - Status badges</li>
  </ul>
</section>

<section class="section-card">
  <h2>CSS Architecture</h2>
  <p>Hybrid CSS architecture combining semantic HTML with light component classes:</p>

  <h3>Component Classes</h3>
  <ul>
    <li><code>.section-card</code> - White content boxes with shadow and border</li>
    <li><code>.section-intro</code> - Light gray intro/description boxes</li>
    <li><code>.card-grid</code> - Grid layout for navigation cards</li>
    <li><code>.card</code> - Individual card styling with hover effects</li>
    <li><code>.link-list</code> - Styled lists with hover states and badges</li>
    <li><code>.site-header</code> - Header layout and spacing</li>
    <li><code>.site-nav</code> - Navigation menu styling</li>
  </ul>

  <h3>Utility Classes</h3>
  <ul>
    <li><code>.half-width</code> - Two-column card grid layout</li>
    <li><code>.full-width</code> - Full-width grid items</li>
    <li><code>.badge</code> - Badge base styling</li>
    <li><code>.success</code>, <code>.warning</code>, <code>.danger</code> - Semantic color variants</li>
  </ul>

  <h3>CSS Features</h3>
  <ul>
    <li>CSS Custom Properties for theming and colors</li>
    <li><code>color-mix()</code> for automatic background/border tints</li>
    <li><code>grid-template-rows</code> for smooth CSS-only accordion animations</li>
    <li>Native <code>&lt;details&gt;</code> with <code>name</code> attribute for exclusive accordions</li>
    <li>Minimal JavaScript - only for badge visibility toggles</li>
  </ul>
</section>

<section class="section-card">
  <h2>Table Styling</h2>
  <p>Semantic table styling with subtle borders and proper spacing</p>

  <table>
    <thead>
      <tr>
        <th>Component</th>
        <th>Purpose</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>FAQ List</td>
        <td>Display question links</td>
        <td>✅ Complete</td>
      </tr>
      <tr>
        <td>Accordion</td>
        <td>Expandable content</td>
        <td>✅ Complete</td>
      </tr>
      <tr>
        <td>Tables</td>
        <td>Structured data display</td>
        <td>✅ Complete</td>
      </tr>
    </tbody>
  </table>
</section>

<section class="section-card">
  <h2>ORC WG Brand Guidelines</h2>
  <p>For the official ORC Working Group logo and brand usage guidelines, check the <a href="https://www.eclipse.org/org/artwork/guidelines/orc-branding-guidelines.pdf">ORC Brand Guidelines (PDF)</a> provided by the Eclipse Foundation</p>
</section>
