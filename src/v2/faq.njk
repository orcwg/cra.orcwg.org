---
layout: v2/layout.njk
pagination:
  data: data.faqItems
  size: 1
  alias: item
permalink: "/v2{{ item.permalink }}"
eleventyComputed:
  title: "{{ item.pageTitle }}"
---

<article class="section-card">
  <h1 class="section-header">{{ item.question | markdownInline | safe }}</h1>
  {{ item.answer | markdown | safe }}

  {% if item.status == "draft" or item.relatedGuidanceRequest %}
  <aside class="warning">
    {% if item.status == "draft" %}
    <p><strong>This FAQ is a draft</strong> - it hasn't been reviewed by the FAQ Task Force. The answer may be incomplete or incorrect.</p>
    {% endif %}

    {% if item.relatedGuidanceRequest %}
    <p><strong>Pending Guidance</strong></p>
    {% if item.relatedGuidanceRequest.guidanceText %}
      <p>This FAQ awaits commission guidance on:</p>
      <blockquote>{{ item.relatedGuidanceRequest.guidanceText }}</blockquote>
      <p><a href="/v2{{ item.relatedGuidanceRequest.permalink }}">View full guidance request →</a></p>
    {% else %}
      <p>This FAQ awaits commission guidance.</p>
    {% endif %}
    {% endif %}
  </aside>
  {% endif %}
</article>

<details>
  <summary>Admin Information</summary>
  <div>
    {% if item["guidance-id"] %}
    <p><strong>Related Pending Guidance:</strong> <a href="/v2{{ item.relatedGuidanceRequest.permalink }}">{{ item["guidance-id"] | replace('-', ' ') | title }}</a></p>
    <p><em>This FAQ is affected by pending guidance that is currently being developed.</em></p>
    {% endif %}

    {% if item["Related issue"] %}
    <p><strong>Related issue:</strong> <a href="{{ item["Related issue"] }}">{{ item["Related issue"] }}</a></p>
    {% endif %}

    <p><a href="{{ item.editOnGithubUrl }}" target="_blank" rel="noopener">✏️ Edit this FAQ on GitHub</a></p>
  </div>
</details>
