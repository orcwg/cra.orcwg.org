---
layout: v2/layout.njk
title: Style Guide V2
permalink: "/v2/style-guide/"
eleventyExcludeFromCollections: true
---

<section>
  <p><strong>Living Style Guide V2</strong> - This page demonstrates all design patterns and components using semantic HTML and the new v2 stylesheet.</p>
  <p>Edit <code>_data/style-guide-content.md</code> or <code>_data/styleGuideData.js</code> to update examples.</p>
</section>

<h2>Markdown Content Examples</h2>

<article>
  {{ styleGuideData.markdownContent | markdown | safe }}
</article>

<h2>FAQ List Component (Simple Links)</h2>
<p>Component: <code>v2/components/faq-list-simple.njk</code> | Data: <code>styleGuideData.sampleListFaqs</code></p>

{% set componentData = {
  title: "Getting Started Questions",
  description: "Basic questions about the Cyber Resilience Act",
  icon: "🚀",
  faqs: styleGuideData.sampleListFaqs
} %}
{% include "v2/components/faq-list-simple.njk" %}

<h2>FAQ Accordion Component</h2>
<p>Component: <code>v2/components/faq-accordion.njk</code> | Data: <code>styleGuideData.sampleAccordionFaqs</code></p>

<article>
  {% set componentData = {
    title: "Technical Questions",
    description: "Detailed technical questions with expandable answers",
    icon: "🔧",
    faqs: styleGuideData.sampleAccordionFaqs
  } %}
  {% include "v2/components/faq-accordion.njk" %}
</article>

<h2>Status Badges</h2>
<p>Component: <code>v2/components/faq-badges.njk</code> | Data: <code>styleGuideData.sampleBadgeFaqs</code></p>

<section>
  <p>Status badges appear on FAQs to indicate their review state. They use <code>&lt;data&gt;</code> elements with semantic values.</p>

  {% for faq in styleGuideData.sampleBadgeFaqs %}
    <section>
      <ul>
        <li>
          <a href="#">
            {{ faq.question }}
            {% include "v2/components/faq-badges.njk" %}
          </a>
        </li>
      </ul>
    </section>
  {% endfor %}
</section>

<h2>Navigation Cards</h2>
<p>Used on home page and list indexes with hover effects and arrows.</p>

<nav>
  <a href="#">
    <h2>Example Card 1</h2>
    <p>This is a navigation card with hover effects and an animated arrow</p>
  </a>
  <a href="#">
    <h2>Example Card 2</h2>
    <p>Cards are created with simple <code>&lt;nav&gt;</code> and <code>&lt;a&gt;</code> elements</p>
  </a>
  <a href="#">
    <h2>Example Card 3</h2>
    <p>No classes needed - all styling comes from semantic selectors</p>
  </a>
</nav>

<h2>Disclaimer/Warning Block</h2>
<p>Uses semantic <code>&lt;aside&gt;</code> element.</p>

<aside class="warning">
  <h3>Important Notice</h3>
  <p><strong>This is an example warning block.</strong> It uses an <code>&lt;aside&gt;</code> element with automatic styling for disclaimers and important notices.</p>
</aside>

<h2>Details/Summary (Native Accordion)</h2>
<p>V2 uses native HTML <code>&lt;details&gt;</code> and <code>&lt;summary&gt;</code> elements instead of custom JavaScript.</p>

<details>
  <summary>Click to expand this native accordion</summary>
  <article>
    <p>This content is inside a <code>&lt;details&gt;</code> element. It uses browser-native expand/collapse behavior.</p>
    <p>The arrow rotation and styling are handled purely with CSS - no JavaScript required!</p>
  </article>
</details>

<details>
  <summary>Another accordion example</summary>
  <article>
    <p>Multiple accordions can be used on the same page.</p>
    <p>Each operates independently without any custom code.</p>
  </article>
</details>

<h2>Semantic HTML Structure</h2>

<section>
  <h3>V2 Markup Philosophy</h3>
  <p>The V2 templates use semantic HTML5 elements with zero classes:</p>
  <ul>
    <li><code>&lt;header&gt;</code> - Site header with logo and navigation</li>
    <li><code>&lt;nav&gt;</code> - Navigation menus and card grids</li>
    <li><code>&lt;main&gt;</code> - Main content area</li>
    <li><code>&lt;section&gt;</code> - Content sections and FAQ categories</li>
    <li><code>&lt;article&gt;</code> - FAQ answers and standalone content</li>
    <li><code>&lt;aside&gt;</code> - Disclaimers and warnings</li>
    <li><code>&lt;footer&gt;</code> - Site footer</li>
    <li><code>&lt;details&gt;</code> + <code>&lt;summary&gt;</code> - Accordions</li>
    <li><code>&lt;data&gt;</code> - Badges with semantic values</li>
  </ul>
</section>

<h2>CSS Architecture</h2>

<section>
  <h3>Semantic Selectors</h3>
  <p>All styles use element selectors, pseudo-classes, and combinators:</p>
  <ul>
    <li><code>body > header</code> - Direct child selector for page header</li>
    <li><code>main > section</code> - FAQ category sections</li>
    <li><code>main > nav > a</code> - Navigation cards</li>
    <li><code>details[open]</code> - Attribute selector for open accordions</li>
    <li><code>summary::before</code> - Pseudo-element for accordion arrow</li>
    <li><code>a:hover</code> - Hover states throughout</li>
  </ul>
  <p><strong>Zero classes</strong> - All styling is based on document structure and semantic meaning.</p>
</section>

<script>
// Make badges visible for demo
document.addEventListener('DOMContentLoaded', function() {
  document.querySelectorAll('[hidden]').forEach(el => {
    if (el.querySelector('data')) {
      el.removeAttribute('hidden');
      el.style.display = 'inline-flex';
      el.style.gap = '8px';
      el.style.marginLeft = '10px';
    }
  });
});
</script>
